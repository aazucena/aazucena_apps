// Root eslint config used by lint-staged pre-commit hook.
// Per-package linting with full rule sets is handled by each package's own
// eslint.config.js via `pnpm lint:packages`.
/** @type {import("eslint").Linter.Config[]} */
export default [];
