/**
 * Hooks Index
 * Portfolio-specific hooks (all others live in @aazucena/hooks)
 */

// useSectionRegistry — portfolio-specific: wires BASE_REGISTRY to the 8 section components
export * from "./useSectionRegistry";
