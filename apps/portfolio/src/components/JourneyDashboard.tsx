/**
 * Journey Dashboard Component — DEBUG SKELETON v2
 * All lazy imports removed to confirm error is in lazy() boilerplate
 */

export function JourneyDashboard(_props: Record<string, unknown>) {
  return null;
}
