{
  "name": "aazucena_apps",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "web:dev": "pnpm dev --filter *portfolio*",
    "cms:dev": "pnpm dev --filter *directus*",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.2"
  },
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "core-js-pure",
      "esbuild",
      "sharp"
    ],
    "overrides": {
      "react-reconciler": "0.34.0-canary-71b3a03c-20251021"
    }
  }
}
