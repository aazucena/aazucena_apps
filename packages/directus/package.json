{
  "name": "extensions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "ls -al && cat ./install.sh && bash ./install.sh"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1"
  }
}